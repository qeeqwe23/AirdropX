# GUI reconstruction notes

The original Python GUI source was not present in the archived repository snapshot, so the PyQt6 front end was reconstructed from the retained UI documentation/reference image.

Software v1.0 intentionally fixes the controller backend to Physics-MPC v1.3.6-Paper. The GUI does not alter the paper MPC law. It only provides mission configuration, custom longitudinal wind profiles, backend invocation, and result visualization.
